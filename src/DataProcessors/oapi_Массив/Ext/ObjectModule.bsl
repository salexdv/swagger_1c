#Область ОписаниеПеременных

Перем Данные Экспорт;

#КонецОбласти

#Область ПрограммныйИнтерфейс

Функция Коллекция() Экспорт
	
	Возврат Данные;
	
КонецФункции

Функция Вставить(Индекс, Значение) Экспорт
	
	Если Не Обработки.oapi_Сервис.ЭтоРасширенныйТипДанных(Значение) Тогда
		ТекстОшибки = НСтр("ru='В качестве значения должен выступать расширенный тип, например ТипСтруктура, ТипМассив, ТипЧисло.'");
		ВызватьИсключение(ТекстОшибки);
	КонецЕсли;
	
	Данные.Вставить(Индекс, Значение);
	Возврат ЭтотОбъект;
	
КонецФункции

Функция Установить(Индекс, Значение) Экспорт
	
	Если Не Обработки.oapi_Сервис.ЭтоРасширенныйТипДанных(Значение) Тогда
		ТекстОшибки = НСтр("ru='В качестве значения должен выступать расширенный тип, например ТипСтруктура, ТипМассив, ТипЧисло.'");
		ВызватьИсключение(ТекстОшибки);
	КонецЕсли;
	
	Данные.Установить(Индекс, Значение);
	Возврат ЭтотОбъект;
	
КонецФункции

Функция НайтиЗначение(Значение) Экспорт
	
	Возврат Данные.Найти(Значение);
	
КонецФункции

Функция Получить(Индекс) Экспорт
	
	Возврат Данные.Получить(Индекс);
	
КонецФункции

Функция Добавить(Значение) Экспорт

	Если Не Обработки.oapi_Сервис.ЭтоРасширенныйТипДанных(Значение) Тогда
		ТекстОшибки = НСтр("ru='В качестве значения должен выступать расширенный тип, например ТипСтруктура, ТипМассив, ТипЧисло.'");
		ВызватьИсключение(ТекстОшибки);
	КонецЕсли;
	
	Данные.Добавить(Значение);
	Возврат ЭтотОбъект;
	
КонецФункции

Функция Удалить(Значение) Экспорт
	
	Данные.Удалить(Значение);
	Возврат ЭтотОбъект;
	
КонецФункции

Функция Количество() Экспорт
	
	Возврат Данные.Количество();
	
КонецФункции

Функция Очистить() Экспорт
	
	Данные.Очистить();
	Возврат ЭтотОбъект;
	
КонецФункции

#КонецОбласти

#Область Инициализация

Данные = Новый Массив();

#КонецОбласти